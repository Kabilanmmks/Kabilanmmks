medium program...

def prime_number(limit):
    prime = []
    for num in range(2, limit + 1):
        primes = True
        for prime in primes:
            if num % prime == 0:
                is_prime = False
                break
        if is_prime:
            primes.append(num)
            yield num

limit = int(input("Enter the limit for prime numbers generation: "))
if limit < 2:
    print("Prime numbers start from 2. Enter a valid limit.")
else:
    prime_generator = generate_primes(limit)
    print(list(prime_generator))
```